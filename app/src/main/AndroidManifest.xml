<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Geofence receiver -->


    <application
        android:label="Mind Anchor"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        <receiver android:name=".GeofenceReceiver" android:exported="true" />

    <activity
            android:name="com.example.geoalert.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- app/src/main/AndroidManifest.xml (inside <application>) -->
        <activity android:name="com.example.geoalert.GeoAlertActivity" />


    </application>

</manifest>
